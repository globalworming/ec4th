todo:
- interpretation semantics of s"
- interpreter: don't execute compile only word
- only print millis and unused when not more input is coming?
- cross compiled 2constant is not working
- have boot as command / means to reset forth completely
- fix abort", no flag in standard
- character parsing, according to standard: 'c'
- move cross
- detect when dictionary is full, see pad
- detect stack overflow and underflow
- DOES>
- abort" does not take flag, have ?abort", https://forth-standard.org/standard/exception/ABORTq
- Make no header flag work / use Alias instead of defer
- speed: check .h / implement lshift / rshift
- speed: have false, true and 0 as word / primitive
- speed: implement parse-word, skip, scan, tolower in assembler to speed up interpreter/compiler
- should we integrate tolower in parse-word?
- speed: improve donext by one cycle with a macro
- speed: todigit in assembler
- speed: flip ram/rom mapping to get rid of pm masking?
